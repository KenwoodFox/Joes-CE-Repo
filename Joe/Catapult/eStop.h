/* here is defined the estop header
 * this estop task allows us to crash 
 * the robot command when a estop situation
 * is detected
 */
 
 
task eStop()
{
	/* example config:
	 * if(limitSwitch == true)
	 * {
	 *	 error = true;
	 * }
	 *
	 * if(error == true)
	 * {
	 *	 //stop all motors blayt;
	 * }
	 */
}