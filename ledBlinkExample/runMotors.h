void runMotors()
{
	//int runSpeed = 88; //the speed the motors will run

	//turn one revolution
	delay(2000);
	if(limitSwitch != true)
	{
		//setMultipleMotors(0, portMotor, starboardMotor); //stop
	}
}
